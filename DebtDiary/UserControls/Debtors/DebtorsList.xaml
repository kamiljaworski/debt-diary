<UserControl x:Class="DebtDiary.DebtorsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DebtDiary"
             mc:Ignorable="d" Width="280"
             d:DesignHeight="240" d:DesignWidth="260">
    <Grid DataContext="{x:Static local:DebtorsListDesignModel.Instance}" Background="{StaticResource DarkGreyBrush}">
        <!-- TODO: Custom scroll bar style -->
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Debtors}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:DebtorsListItem/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
